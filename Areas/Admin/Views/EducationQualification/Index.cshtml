@model IEnumerable<Abundance_Nk.Model.Entity.EDUCATIONAL_QUALIFICATION>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Educational_Qualification_Abbreviation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Educational_Qualification_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Educational_Qualification_Abbreviation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Educational_Qualification_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Educational_Qualification_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Educational_Qualification_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Educational_Qualification_Id })
        </td>
    </tr>
}

</table>
